# المساهمة في MushafImad

**السلام عليكم ورحمة الله وبركاته**

أهلاً بك في **MushafImad**. ما تكتبه من كود وتوثيق واختبارات هو صدقة جارية ينتفع بها المسلمون
القارئون للقرآن في كل مكان.

هدفنا ليس كتابة الكود فحسب، بل بناء مجتمع من المتقنين يصنعون برمجيات عالية الجودة تدوم — ويبقون
معها.

> **In English:** [CONTRIBUTING.md](CONTRIBUTING.md)

## آداب المساهمة (سير العمل)

حرصاً على الجودة ومنعاً لتضييع الجهد، نتبع هذا المسار بدقة. نرجو الالتزام بخطواته:

1. **تواصل أولاً**: قبل كتابة أي كود، تصفّح
   [المهام المفتوحة (Issues)](https://github.com/ibo2001/MushafImad/issues). إن وجدت مهمة تناسبك،
   أو كانت لديك فكرة جديدة، اترك تعليقاً على الإيشو.
   * *مثال: «السلام عليكم، أرغب بالعمل على هذه المهمة، هل هي متاحة؟»*
2. **انتظر التعيين**: لا تبدأ العمل قبل أن يعيّن لك مشرف المشروع المهمة رسمياً. هذا يمنع أن يعمل
   شخصان على المهمة نفسها. **طلبات السحب التي تصل دون تعليق مسبق ودون تعيين تُغلق عادةً بلا مراجعة.**
3. **انسخ المستودع وأنشئ فرعاً**:
   * انسخ (Fork) المستودع إلى حسابك على GitHub.
   * أنشئ فرعاً خاصاً بمهمتك (مثل `feature/search-ui` أو `fix/typo-readme`).
     **لا تعمل على `main` مباشرة.**
4. **طوّر واختبر**: اكتب الكود، وشغّل الاختبارات الموجودة، وأضف اختبارات جديدة.
5. **أرسل طلب سحب (PR)**:
   * ارفع فرعك إلى نسختك من المستودع.
   * **افتح الطلب باتجاه فرع `main` في MushafImad.** `main` هو فرع الدمج الوحيد، ولا يوجد فرع
     `develop`.
   * **مهم**: أشر إلى رقم الإيشو في وصف الطلب (مثل: "Fixes #12").
6. **المراجعة**: سيقوم المشرف بالموافقة، أو طلب تعديلات، أو توضيح سبب تعذّر قبول الطلب. الملاحظة
   البنّاءة هديّة — توقّع جولة أو جولتين منها.
7. **بعد الدمج**: اكتب منشوراً قصيراً عن تجربتك في [مجتمع إتقان](https://community.itqan.dev): ماذا
   بنيت، وماذا تعلّمت، وما الذي واجهك. هذه ليست خطوة شكلية، بل هي الطريق الذي يجد به المساهم التالي
   مدخله إلى المشروع.

## استخدام أدوات الذكاء الاصطناعي

أدوات الذكاء الاصطناعي مرحّب بها بوصفها **أدوات**، لا بوصفها مؤلِّفاً.

* **يمكنك** استخدامها لاستكشاف الكود، أو صياغة مسوّدة، أو كتابة اختبارات، أو تحسين الصياغة.
* **يجب** أن تفهم كل سطر ترسله وأن تكون قادراً على تفسير سبب كتابته بهذه الطريقة. إذا سأل المراجع
  «لماذا هذا الأسلوب؟» فإن جواب «لا أعرف، النموذج كتبه» غير مقبول، ويُغلق الطلب.
* **نرجو الإشارة** في وصف الطلب إذا كتب الذكاء الاصطناعي جزءاً كبيراً من التغيير. هذا لا يُحسب
  عليك، وإنما يدل المراجع على المواضع التي تحتاج تدقيقاً أعمق.
* **لا** ترسل طلبات سحب آلية أو عابرة. الطلبات المولّدة آلياً بلا تعيين مسبق تُغلق مباشرة مهما كانت
  جودتها.

القاعدة خلف ذلك كله: الغاية من المساهمة هنا أن تعرف *أنت* المشروع معرفة تؤهلك للبقاء في صيانته.
ومخرجات لا يفهمها بشر لا تحقق هذه الغاية.

## البداية

### المتطلبات

* macOS 14 أو أحدث
* **Xcode 16 أو أحدث** (الحزمة على `swift-tools-version: 6.0` وتُبنى بنمط لغة Swift 6)
* أهداف النشر: **iOS 17+ / macOS 14+**

### الإعداد

1. استنسخ نسختك من المستودع:
   ```bash
   git clone https://github.com/YOUR_USERNAME/MushafImad.git
   cd MushafImad
   ```
2. افتح المشروع:
   * انقر نقراً مزدوجاً على `Package.swift` لفتحه في Xcode كحزمة.
   * **أو** افتح `Example/Example.xcodeproj` لتشغيل التطبيق التجريبي على محاكٍ.
3. تستخدم الحزمة قاعدة بيانات Realm مضمّنة (`quran.realm`) وقرابة 110 ميغابايت من صور الصفحات، وكلاهما
   من موارد الحزمة ويُدار تلقائياً — لكن انتظر انتهاء الفهرسة (Indexing) في Xcode قبل التشغيل، وتوقّع
   أن يكون الاستنساخ الأول كبير الحجم.

### البناء والاختبار

```bash
swift build
swift test --no-parallel
```

**شغّل الاختبارات بالتتابع.** الخيار `--no-parallel` ليس اختيارياً: المجموعات تتشارك كائنات مفردة
(`RealmService.shared` والذواكر المؤقتة)، فالتشغيل المتوازي غير مستقر ويغطي أقل مما يبدو.

الاختبارات مكتوبة بـ **swift-testing** (`import Testing`, `@Test`, `#expect`) وليس XCTest، فنرجو
اتباع ذلك في أي اختبار جديد.

بعض اختبارات تتبّع العين مسجّلة كـ **مشكلات معروفة (known issues)**: تعمل، وتفشل، وتبقى المجموعة
خضراء. وإذا **نجح** أحدها فسيفشل التشغيل عن قصد، لأن ذلك إشارة إلى أن الخلل الأصلي قد أُصلح وأن
العلامة يجب أن تُزال. لا تحذف علامة المشكلة المعروفة لمجرّد جعل الاختبارات خضراء.

### أمور تستحق المعرفة قبل إرسال الطلب

* **هذه مكتبة.** كل رمز `public` هو عقد مع تطبيقات منشورة فعلاً. تغيير أي توقيع عام أو حذفه يُعدّ
  تغييراً كاسراً، ويجب ذكره صراحة في وصف الطلب.
* **كائنات Realm تُعاد مجمّدة (frozen).** المجمّدة تعبر الخيوط (threads) بأمان، والحيّة لا. إعادة
  كائن غير مجمّد خللٌ كامن يظهر في تطبيق أحدهم لاحقاً.
* **التسجيل يمر عبر `AppLogger.shared`** مع تصنيف مناسب. نرجو عدم إضافة `print`.
* **النصوص الظاهرة للمستخدم** تستخدم `String(localized:)` ومكانها `Resources/Localizable.xcstrings`.
* **الكود الخاص بمنصّة بعينها** يُحاط بـ `#if canImport(UIKit)` / `#elseif canImport(AppKit)`
  للأنواع، وبـ `#if os(iOS)` للسلوك.
* **رسائل الـ commit** تتبع [Conventional Commits](https://www.conventionalcommits.org/)
  (`feat:`, `fix:`, `docs:`, `test:`, `ci:`, `refactor:`).

### بنية المشروع

* `Sources/MushafImad/` — واجهات SwiftUI (`MushafView`, `MushafTextView`, وعروض الصفحة)
* `Sources/MushafImad/Core/` — النماذج وخدمات Realm والذواكر المؤقتة والامتدادات
* `Sources/MushafImad/AudioPlayer/` — التلاوة: منسّق المشغّل، والتوقيتات، والقرّاء، والواجهات
* `Sources/MushafImad/EyeTracking/` — **تجريبي**، اختياري، وغير مكتمل بشكل معروف
* `Tests/MushafImadSPMTests/` — مجموعة الاختبارات
* `Example/` — تطبيق تجريبي لنظامي iOS وmacOS

### أدوات وقت البناء (Build-time tooling)

توجد سكربتات في جذر المستودع لتوليد الموارد بصورة قابلة للتكرار (reproducible)، وهي **لا** تُشحن داخل الحزمة البرمجية.

| السكربت | الغرض |
| --- | --- |
| `configure_example_project.sh` | إعداد مشروع Xcode التجريبي للوصول إلى الشبكة |
| `compose_page_images.swift` | دمج 15 صورة سطر لكل صفحة في 604 صورة صفحة كاملة |

#### تجميع صور الصفحات

تتضمن الحزمة 604 × 15 صورة أسطر منفردة (`Sources/MushafImad/quran-images/<page>/<line>.png` بحجم 1440×232 لكل سطر). لتوليد صور الصفحات الكاملة:

```bash
./compose_page_images.swift [options]
# أو: swift compose_page_images.swift [options]
```

الخيارات المتاحة:
- `--page` أو `-p <1..604>`: تجميع صفحة واحدة محددة (مفيد للتجربة السريعة).
- `--all` أو `-a`: تجميع كل الصفحات الـ 604 (الوضع الافتراضي).
- `--lines-dir <path>`: مسار مخصص لمجلد صور الأسطر المدخلة.
- `--output-dir <path>`: مسار مخصص لمجلد المخرجات (الافتراضي: `build/page-images/`).
- `--repo-root <path>`: مسار جذر المستودع.
- `--help` أو `-h`: عرض تعليمات الاستخدام.

يتم حفظ المخرجات في `build/page-images/<page>.png` (صور RGBA PNG بأبعاد 1440×2603). تُركّب الأسطر وفق التباعد الفعلي للقارئ البالغ **169.36px** (`lineHeight * 0.73`)، حيث تتداخل الأسطر المتتالية رأسياً بنسبة 27% بما يطابق تركيب القارئ نفسه. ينتج عن ذلك تطابق بصري دقيق بنسبة 1:1 مع ما يعرضه `QuranPageView` على الشاشة في **الوضع العمودي (portrait)** (أما الوضع الأفقي فيستخدم `lineHeight * 0.7 = 162.4px`).

##### ثنائية الأبعاد لمصادر الرسم (#70)
- **ارتفاع الصورة البصري (Visual image height)**: `2603px` (`14 × 169.36 + 232 = 2603.04`). يمتد السطر 15 طبيعياً خارج خانته بمقدار 62.6px فوق عنصر المباعدة (`Spacer()`) أسفله.
- **ارتفاع التخطيط (Layout height)**: `2540.4px` (`15 × 169.36`) وهو ما يستهلكه `VStack(spacing: 0)`.
- **إرشادات الرسم**: يجب على مصادر الرسم اللاحقة رسم الصورة المركبة بارتفاع `height = width * 2603 / 1440` بمحاذاة السطر الأول من الأعلى — *وليس* احتواؤها داخل صندوق التخطيط `2540.4` (مما قد يقلص الصفحة بنسبة 2.4% ويُخل بتسجيل إحداثيات الضغط والتظليل للأسطر).

##### نتائج الحجم والضغط (#69)
صور الصفحات **محايدة الحجم (size-neutral)**؛ ومضاعفة الحجم الظاهرة على القرص (~2.8x) تعود فقط إلى محول التشفير الافتراضي. فنظراً لأن صور الأسطر الأصلية هي أقنعة سوداء خالصة (`maxRGB = (0,0,0)`) مع قناة شفافية، فإن كامل المعلومات محتواة في قناة الشفافية فقط. وعند قياس محتوى قناة الشفافية المضغوط (gzipped)، يكون الحجم متطابقاً تقريباً أو أصغر قليلاً كصورة صفحة واحدة (~0.98x). محول `CGImageDestination` في نظام macOS يكتب صور 32-bit RGBA8 غير مضغوطة (مخزناً 3 بايتات ألوان صفرية لكل بكسل)، بينما تستخدم صور الأسطر الأصلية ضغط الفهرسة (palette). وإعادة ضغطها بأدوات مثل `oxipng` أو حفظها كأقنعة رمادية/شفافية 8-bit يعيد حجم الملفات على القرص إلى مكافئ حجم صور الأسطر.

السكربت حتمي (deterministic): تشغيله مرتين على نفس المدخلات يُنتج ملفات متطابقة بايت لبايت، مع الحفاظ على قناة الشفافية (alpha) المطلوبة لتلوين السمات عبر `.renderingMode(.template)`. هذا المجلد تم استثناؤه في `.gitignore` لتجنب إضافة ملفات ثنائية كبيرة للمستودع.

## ميثاق السلوك

نلتزم بأصول الأخوّة الإسلامية: الرفق والاحترام والصبر. والملاحظة البنّاءة هديّة.

**جزاكم الله خيراً** على وقتكم وجهدكم.
